// Copy this file to firebase-config.js and fill in your own Firebase web app
// config (Firebase console > Project settings > Your apps).
//
// firebase-config.js is gitignored. On Netlify it is generated at build time
// from the FIREBASE_API_KEY environment variable, see netlify.toml.
//
// The placeholder values below are enough to run the app as a guest: sign-in
// will fail and the app falls back to storing everything in this browser.
export const firebaseConfig = {
  apiKey: "your-api-key",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project",
  storageBucket: "your-project.firebasestorage.app",
  messagingSenderId: "000000000000",
  appId: "1:000000000000:web:0000000000000000000000",
  measurementId: "G-0000000000"
};
